# Ключи и сессии. Сюда попадают токен бота, api_id/api_hash и файл сессии,
# который даёт полный доступ к аккаунту, — в репозитории им делать нечего.
data/
*.session
*.session-journal

# Локальное окружение и мусор
.venv/
__pycache__/
*.pyc
.DS_Store
.claude/
tmp_*.py
